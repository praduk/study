For all sets $A$ and $B$,

$$
\bigl[\forall x\,(x\in A\leftrightarrow x\in B)\bigr]\to A=B.
$$

The converse follows from equality, so two sets are equal exactly when they have the same members. Extensionality gives uniqueness for constructions specified by their membership condition; it does not by itself assert that any such set exists.
