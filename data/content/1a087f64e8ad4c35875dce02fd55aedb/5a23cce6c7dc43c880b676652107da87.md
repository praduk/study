Every nonempty set has an $\in$-minimal member:

$$
\forall A\,[A\ne\varnothing\to
\exists x\in A\,(x\cap A=\varnothing)].
$$

Foundation rules out membership loops such as $x\in x$ and finite cycles. Its deeper role is to support induction and recursion along membership and to place every set in the cumulative hierarchy.
