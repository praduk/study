Let $\theta$ be an ordinal and $X\subseteq\theta$. If for every $\alpha<\theta$,

$$
\alpha\subseteq X\to\alpha\in X,
$$

then $X=\theta$. Equivalently, to prove $P(\alpha)$ for every $\alpha<\theta$, it suffices to derive $P(\alpha)$ from $P(\beta)$ for all $\beta<\alpha$.
