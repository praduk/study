The declared calculus derives each schema below:

1. Identity: $\vdash A\to A$.
2. Double-negation elimination: $\neg\neg A\vdash A$.
3. Excluded middle: $\vdash A\lor\neg A$.
4. Modus tollens: $A\to B,\neg B\vdash\neg A$.
