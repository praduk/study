Suppose a property of terms holds for variables and constants and is preserved when $f(t_1,…,t_n)$ is formed. Then it holds for every term. Suppose a property of formulas holds for atomic formulas and $⊥$ and is preserved by every connective and by both quantifiers. Then it holds for every formula.
