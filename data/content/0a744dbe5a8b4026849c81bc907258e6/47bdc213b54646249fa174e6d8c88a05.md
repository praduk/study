Let $G=(V,E)$ be a set-sized undirected graph. Order by inclusion the set of all acyclic subsets of $E$.

1. Use Zorn's Lemma to prove that every acyclic edge set is contained in a maximal acyclic edge set.
2. If $G$ is connected, prove that any maximal acyclic edge set is the edge set of a spanning tree.
