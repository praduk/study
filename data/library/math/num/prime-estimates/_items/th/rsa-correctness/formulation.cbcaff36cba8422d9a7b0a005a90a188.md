Let $p,q$ be distinct primes, $n=pq$, and choose positive integers $e,d$ with $ed\equiv1\pmod{(p-1)(q-1)}$. For every integer message $m$,

$$
(m^e)^d\equiv m\pmod n.
$$

This is an arithmetic correctness statement, not a security guarantee for unpadded encryption.
