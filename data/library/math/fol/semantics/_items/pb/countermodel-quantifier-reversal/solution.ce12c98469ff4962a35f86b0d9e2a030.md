No. Let the domain be $M=\{0,1\}$ and interpret $R(x,y)$ as identity: $R^𝔐=\{(0,0),(1,1)\}$. For each $x∈M$, choose $y=x$; hence $𝔐⊨∀x∃yR(x,y)$. But no single $y$ is related to both $0$ and $1$: for $y=0$, $R(1,0)$ fails, and for $y=1$, $R(0,1)$ fails. Thus $𝔐⊭∃y∀xR(x,y)$, so the proposed consequence is invalid.
