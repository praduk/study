The set of valid first-order sentences is recursively enumerable: there is an algorithm that eventually lists every valid sentence and lists no invalid sentence. Equivalently, validity is semidecidable.
