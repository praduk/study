Replace $y$ by a new unary function $f(x)$ because only $x$ precedes it universally. Replace $w$ by a new binary function $g(x,z)$ because $x$ and $z$ precede it universally. The result is $∀x∀z\,R(x,f(x),z,g(x,z))$.

The original sentence in the old language is satisfiable exactly when this sentence is satisfiable in the expanded language. A model of the new sentence reduces to a model of the old one, and a model of the old sentence has at least one suitable expansion after witnesses are chosen. The two displayed sentences are not logically equivalent across arbitrary interpretations of $f$ and $g$.
