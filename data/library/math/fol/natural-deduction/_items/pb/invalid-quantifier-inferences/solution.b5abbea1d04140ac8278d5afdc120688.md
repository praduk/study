(a) is invalid. On domain $\{0,1\}$ let $P$ hold only of $0$. The existential is true and the universal false.

(b) is invalid. On the same domain let $P$ hold only of $0$ and $Q$ only of $1$. Every object satisfies one disjunct, but neither predicate holds universally.

(c) violates universal introduction because $x$ is free in the undischarged assumption on which the line depends. Semantically, an assignment may make $P(x)$ true by sending $x$ to a special object even though $P$ fails elsewhere.
