For $n>m$, Cauchy's coefficient estimate at zero gives
$|f^{(n)}(0)|/n!\le C(1+r)^m/r^n$ for every $r>0$.
The bound tends to zero as $r\to\infty$, hence $f^{(n)}(0)=0$. The entire Taylor expansion about zero therefore has no terms above degree $m$, proving the claim.
