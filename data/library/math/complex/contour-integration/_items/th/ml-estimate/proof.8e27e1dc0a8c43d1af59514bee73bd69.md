The triangle inequality for a complex integral follows by multiplying its value by a unit complex number that makes it real and nonnegative, then taking real parts inside the integral. Applying it to the parametrized expression gives
$|\int f(\gamma(t))\gamma'(t)\,dt|\le\int |f(\gamma(t))||\gamma'(t)|\,dt\le M L(\gamma)$.
Sum over the finitely many smooth pieces.
