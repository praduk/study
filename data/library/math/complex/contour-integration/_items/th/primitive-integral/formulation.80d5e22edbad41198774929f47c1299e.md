If $F$ is holomorphic on an open set containing a contour $\gamma$ and $F'=f$ is continuous there, then

$$
\int_\gamma f(z)\,dz=F(\gamma(b))-F(\gamma(a)).
$$

In particular, a function with such a primitive has zero integral around every closed contour.
