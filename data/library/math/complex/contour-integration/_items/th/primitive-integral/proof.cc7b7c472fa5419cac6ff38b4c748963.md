On each smooth piece the real chain rule gives
$(F\circ\gamma)'(t)=F'(\gamma(t))\gamma'(t)=f(\gamma(t))\gamma'(t)$.
The real fundamental theorem of calculus applied to the real and imaginary parts gives the endpoint difference. Adding across adjacent pieces cancels the intermediate endpoint values.
