A formula is in **negation normal form** (NNF) when it is built from literals and the constants $\true,\false$ using only $\land$ and $\lor$. Equivalently, it contains no $\to$ or $\leftrightarrow$, and every occurrence of $\neg$ is immediately before an atom.
