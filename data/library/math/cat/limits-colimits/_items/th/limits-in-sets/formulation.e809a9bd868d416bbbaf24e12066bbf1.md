For a small diagram $D:J\to\Set$, its limit is

$$
L=\{(x_j)\in\prod_{j\in\Obj J}D(j):D(a)(x_i)=x_j\text{ for every }a:i\to j\},
$$

with coordinate projections. In particular $\Set$ has all small limits.
