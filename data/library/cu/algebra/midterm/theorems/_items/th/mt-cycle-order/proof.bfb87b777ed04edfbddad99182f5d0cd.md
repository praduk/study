Starting at any unused symbol, successive images eventually repeat because the set is finite. Bijectivity forces the first repeated point to be the starting one, producing a cycle. Repeat on the unused symbols. These orbits determine the disjoint cycles uniquely. On each orbit a power is the identity exactly when the cycle length divides that exponent, so the least simultaneous positive exponent is the least common multiple. The identity has order one.
