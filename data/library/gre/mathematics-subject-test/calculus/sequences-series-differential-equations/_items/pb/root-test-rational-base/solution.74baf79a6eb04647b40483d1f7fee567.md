**Correct choice: (B).**

**Fast route.** The $n$th root is $(2n+1)/(3n)\to2/3<1$, so the root test gives absolute convergence.

**Trap check.** When the entire term is an $n$th power, the root test removes the exponent immediately.
