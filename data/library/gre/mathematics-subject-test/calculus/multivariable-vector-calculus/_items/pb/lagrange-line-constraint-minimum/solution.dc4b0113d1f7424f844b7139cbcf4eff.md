**Correct choice: (E).**

**Fast route.** The closest point on the line to the origin is $(3,3)$, giving $9+9=18$. Equivalently, Lagrange multipliers force $x=y$.

**Trap check.** The constraint fixes the sum, so equality in the quadratic mean occurs at equal coordinates.
