**Correct choice: (E).**

**Fast route.** Solve $x=(u+v)/2$, $y=(u-v)/2$. The determinant is $-1/2$, so the absolute Jacobian is $1/2$.

**Trap check.** Integration uses the absolute value of the inverse Jacobian requested here.
