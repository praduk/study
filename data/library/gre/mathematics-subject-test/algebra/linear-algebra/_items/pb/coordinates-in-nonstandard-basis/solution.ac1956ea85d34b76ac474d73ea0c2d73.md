**Correct choice: (E).**

**Fast route.** Solve $a(1,1)+b(1,-1)=(5,1)$. Then $a+b=5$ and $a-b=1$, so $(a,b)=(3,2)$.

**Trap check.** Coordinates are coefficients relative to the basis, not the standard components.
