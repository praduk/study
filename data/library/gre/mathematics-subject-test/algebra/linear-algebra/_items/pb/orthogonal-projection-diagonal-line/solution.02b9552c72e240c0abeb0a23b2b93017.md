**Correct choice: (E).**

**Fast route.** The projection coefficient is $(3+4)/(1^2+1^2)=7/2$, so the projection is $(7/2)(1,1)$.

**Trap check.** Divide by $u\cdot u$ unless the spanning vector is already unit length.
