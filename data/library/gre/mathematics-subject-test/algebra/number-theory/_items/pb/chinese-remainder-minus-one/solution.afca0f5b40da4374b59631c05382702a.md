**Fastest valid route.** Each congruence says $n\equiv-1$ modulo its modulus. Since $3$, $5$, and $7$ are pairwise coprime, the Chinese remainder theorem combines them as

$$n\equiv-1\pmod{3\cdot5\cdot7}= -1\pmod{105}.$$

The smallest positive representative is $104$, so the answer is **(E)**.
