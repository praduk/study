**Correct choice: (E).**

**Fast route.** Each edge contributes $1$ to the degree of each endpoint, so the handshaking lemma gives $2|E|=30$.

**Trap check.** Even a loop, under the standard convention, contributes two to degree and preserves the lemma.
