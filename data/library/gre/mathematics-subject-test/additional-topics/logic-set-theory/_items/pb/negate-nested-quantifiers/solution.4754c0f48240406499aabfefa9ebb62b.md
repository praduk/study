**Correct choice: (B).**

**Fast route.** Negation reverses each quantifier and negates the predicate: $\forall$ becomes $\exists$, then $\exists$ becomes $\forall$.

**Trap check.** Both quantifiers must reverse; changing only the final predicate is insufficient.
