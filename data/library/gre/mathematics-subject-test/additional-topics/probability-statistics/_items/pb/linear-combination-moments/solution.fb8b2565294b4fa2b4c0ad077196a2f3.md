**Correct choice: (A).**

**Fastest valid route.** Linearity gives

$$
E[Z]=2(2)-3(-1)=7.
$$

Independence makes the covariance zero, so coefficient squares govern the variance:

$$
\operatorname{Var}(Z)=2^2(3)+(-3)^2(4)=12+36=48.
$$

**Verification.** In full,

$$
\operatorname{Var}(2X-3Y)
=4\operatorname{Var}(X)+9\operatorname{Var}(Y)-12\operatorname{Cov}(X,Y).
$$

Since $X$ and $Y$ are independent, $\operatorname{Cov}(X,Y)=0$, confirming $(E[Z],\operatorname{Var}(Z))=(7,48)$.
