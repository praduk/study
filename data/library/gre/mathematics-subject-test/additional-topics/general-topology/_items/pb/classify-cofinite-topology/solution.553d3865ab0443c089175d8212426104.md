**Correct choice: (B).**

**Fastest valid route.** Singletons are closed because their complements are cofinite, so the space is $T_1$. Two nonempty open sets cannot be disjoint: the complement of their intersection is a finite union of finite sets, so their intersection is nonempty because $X$ is infinite. Thus the space is not Hausdorff.

**Verification.** Given an open cover, choose one nonempty member $U$. Its complement $X\setminus U$ is finite. Choose one set from the cover for each of those finitely many remaining points. Together with $U$, these sets form a finite subcover, proving compactness. The topology is not discrete because a singleton is not open when $X$ is infinite.
