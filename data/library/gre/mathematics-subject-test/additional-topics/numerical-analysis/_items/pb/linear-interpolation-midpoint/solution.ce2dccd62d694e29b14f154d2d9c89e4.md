**Correct choice: (C).**

**Fast route.** Because $2$ is halfway between $1$ and $3$, linear interpolation gives the midpoint of the values: $(2+10)/2=6$.

**Trap check.** Interpolation uses the geometry of the data interval, not an average of the $x$- and $y$-coordinates together.
