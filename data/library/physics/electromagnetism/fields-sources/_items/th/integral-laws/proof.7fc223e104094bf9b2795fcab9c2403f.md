Integrate each divergence equation over $V$ and use the divergence theorem. Integrate each curl equation over $S$ and use Stokes' theorem. Since the surfaces are fixed and the fields are smooth, differentiation under the integral gives the two displayed time derivatives. This proof does not apply unchanged to a moving circuit: the measured electromotive force then includes $\mathbf v_{\rm wire}\times\mathbf B$.
