The logarithm of the ratio is $\log(p_i(\eta)/p_i(\eta+h))=-h^aA_a(i)+\Psi(\eta+h)-\Psi(\eta)$. Its $p_\eta$ average gives the exact identity by @physics:statistical:geometry:th:fisher-covariance. The finite exponential sum makes $\Psi$ smooth; Taylor expansion through order two on a compact neighborhood gives the stated cubic remainder. The linear term cancels exactly. The coordinate change law for $G$ in @physics:statistical:geometry:df:exponential-family shows that the second-order form is invariant. There is no implication that finite relative entropies obey symmetry or the triangle inequality.
